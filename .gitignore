node_modules/
.vite/
*.log

# Build output — never commit, CI deploys automatically
dist/
index.html
# ...but the harness has its own entry page; the rule above is unanchored and
# would otherwise swallow it at any depth.
!harness/index.html

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local

# Brainstorming visual-companion session files (local, ephemeral)
.superpowers/

# Isolated worktrees for subagent-driven development
.worktrees/
